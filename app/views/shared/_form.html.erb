<%= simple_form_for [@category, @recommendation] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :location  %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <br>
  <!-- submit photo button -->
  <%= f.submit "Add Recommendation" %>
<% end %>

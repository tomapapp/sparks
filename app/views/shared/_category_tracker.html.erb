  <div class="category-selection-bar">
    <div class="category-selection-text">
      <h4 class="category-selection-title">What you're interested in:</h4>
      <div class="selected-categories">
        <% @categories.each do |category| %>
          <span class="selected-category btn btn-outline-danger btn-sm"> <%= category.name %></span>
        <% end %>
      </div>
    </div>
    <div>
    </div>
    <%= link_to "Pick more", pick_a_category_path, class: "category-selection-button" %>
  </div>
